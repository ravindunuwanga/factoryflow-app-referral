import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.factoryflow.driver',
  appName: 'FactoryFlow Driver',
  webDir: 'out'
};

export default config;
